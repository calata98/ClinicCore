spring:
  datasource:
    url: jdbc:h2:file:./.local-data/cliniccore;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true
      path: /h2-console

cliniccore:
  bootstrap-demo: true
